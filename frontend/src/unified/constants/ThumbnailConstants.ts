/**
 * 缩略图相关常量
 */
export const THUMBNAIL_CONSTANTS = {
  /** 缩略图宽度（像素） */
  WIDTH: 50,
  
  /** 缩略图高度（像素） */
  HEIGHT: 40,
  
  /** 缩略图容器顶部偏移量（像素） */
  TOP_OFFSET: 5,
  
  /** 可见性计算缓冲帧数 */
  VISIBILITY_BUFFER_FRAMES: 100,
} as const