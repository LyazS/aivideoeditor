/**
 * 统一时间系统常量定义
 * 包含帧率、时间计算等相关常量
 */

// ==================== 帧率相关常量 ====================

/**
 * 固定帧率 - 30fps（用于时间码计算）
 */
export const FRAME_RATE = 30

// ==================== 导出时间常量 ====================

export const TimeConstants = {
  // 帧率相关
  FRAME_RATE,
} as const
